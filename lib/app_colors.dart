import 'package:flutter/material.dart';

/// Smriti palette. Warm ivory ground with the muted colours of North-East
/// India: terracotta and gamosa red, hill-sky indigo, mustard (muga silk),
/// tea-garden green and river teal. Every foreground/background pair used in
/// the app meets WCAG AA for large text.
class AppColors {
  // Ground & Surface
  static const Color pageBackground = Color(0xFFF5EEE2);
  static const Color raisedSurface = Color(0xFFFFFBF5);
  static const Color bottomStrip = Color(0xFFECE2D1);
  static const Color wovenMat = Color(0xFFE4D6BF);
  static const Color border = Color(0xFFE2D5C1);
  static const Color medallion = Color(0xFFF8F1E6);

  // Text
  static const Color primaryText = Color(0xFF26211D);
  static const Color secondaryText = Color(0xFF5E554D);
  static const Color ghostHand = Color(0xFF8C8078);

  // Terracotta
  static const Color terracotta = Color(0xFFBF5537);
  static const Color terracottaDark = Color(0xFF8C3A24);
  static const Color terracottaDeep = Color(0xFF5A2617);
  static const Color terracottaPressed = Color(0xFFA84A30);

  // Indigo (hill sky)
  static const Color indigo = Color(0xFF3C5A88);
  static const Color indigoDark = Color(0xFF243A5E);
  static const Color indigoPressed = Color(0xFF334E77);

  // Marigold (muga silk / mustard)
  static const Color marigold = Color(0xFFD79E34);
  static const Color marigoldDark = Color(0xFF8A6214);
  static const Color marigoldPressed = Color(0xFFC48D2A);

  // Leaf Green (tea garden)
  static const Color leafGreen = Color(0xFF477A55);
  static const Color leafGreenDark = Color(0xFF2D5138);
  static const Color leafGreenPressed = Color(0xFF3D6A49);

  // River Teal
  static const Color riverTeal = Color(0xFF2F6A6D);
  static const Color riverTealDark = Color(0xFF1D4749);

  // Orchid (Meghalaya and Manipur orchids)
  static const Color orchid = Color(0xFF7A4E8C);

  // Bamboo
  static const Color bamboo = Color(0xFF647330);

  // Tea brown
  static const Color teaBrown = Color(0xFF7A5234);

  // Gamosa red, for woven accents
  static const Color gamosaRed = Color(0xFFA8322A);

  // Supporting
  static const Color onColor = Color(0xFFFFF8ED);
  static const Color medicineBlush = Color(0xFFF5E4DC);
  static const Color gamePressed = Color(0xFFF6E7D3);
  static const Color recordingDot = Color(0xFFC7402A);
}
